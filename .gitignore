__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/
build/
dist/

# Generated graphs / trained models / exports
data/*
!data/.gitkeep

# C build artifacts
engine/*.so
engine/*.o
engine/test_engine
engine/test_engine.exe
engine/build/

# mteb's raw per-run dump (overwritten every run, not a history) — the
# timestamped benchmarks/sts_summary_*.json files are the tracked history.
benchmarks/raw/
